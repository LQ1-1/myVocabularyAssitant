<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vocabularyassitant.mapper.UserMapper">

    <select id="getUserByUserId"
            parameterType="string"
            resultType="com.vocabularyassitant.entity.User">
        select * from UserTable where uId=#{uId};
    </select>

    <update id="updateUserInfo">
        update UserTable set uPassword=#{uPassword}, uName=#{uName}, uStyle=#{uStyle} where uId=#{uId};
    </update>

    <insert id="addUser"
            parameterType="com.vocabularyassitant.entity.User">
        insert into UserTable(uId, uPassword, uName, uStatus, uStyle, uUpdated_at)values(
        #{uId}, #{uPassword}, #{uName},
        #{uStatus}, #{uStyle}, #{uUpdated_at}
        );
    </insert>

</mapper>